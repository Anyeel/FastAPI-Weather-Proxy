/**
 * Matches the backend CitySearchResponse model.
 * Represents a simplified city profile used in the autocomplete dropdown.
 */
export interface CitySearchResponse {
  id: number;
  name: string;
  country: string;
}
